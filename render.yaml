services:
  - type: web
    name: bhoomilens
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 server.py
    healthCheckPath: /
    envVars:
      - key: GROQ_API_KEY
        sync: false